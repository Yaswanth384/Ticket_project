import React, { useContext } from 'react'
import tickeContext from '../contexts/tickets/ticketContext'

const About = () => {

    // let {ticket} = useContext(tickeContext)

  return (
    <>
    Hello world { t} . Here is {data}
    </>
  )
}

export default About
